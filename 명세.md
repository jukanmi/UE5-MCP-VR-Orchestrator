<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# 그래 만들어

로컬 VR NPC 전체 아키텍처를 명세한 Markdown을 아래처럼 정리했습니다.
필요하면 프로젝트에 맞게 섹션/필드만 수정해서 바로 계약서처럼 쓰면 됩니다.

```markdown
# 로컬 VR NPC 통합 아키텍처 명세서

> 오픈소스 및 로컬 환경 기반 VR NPC 시스템  
>  
> LLM + (오디오 LLM/ASR) + TTS + 오케스트레이터 + UE5 WebSocket/StateTree 통합 설계

---

## 1. 문서 목적 및 범위

이 문서는 PC/VR 환경에서 **완전 오프라인**으로 구동되는 AI 기반 VR NPC 시스템의 전체 아키텍처를 정의합니다.

- 대상 독자
  - 언리얼 엔진 5(UE5)로 VR 게임/애플리케이션을 개발하는 클라이언트 팀
  - 로컬 LLM/오디오/TTS 서버를 구현·운영하는 백엔드/ML 엔지니어 팀
- 범위
  - LLM, ASR/오디오-네이티브 LLM, TTS, 오케스트레이터, UE5 클라이언트까지 **엔드 투 엔드 파이프라인** 명세
  - WebSocket 기반 메시지 스키마 및 API 규약
  - 단계별(Phase 0–4) 개발 로드맵 및 권장 구현 순서

---

## 2. 시스템 목표 및 비기능 요구사항

### 2.1 기능적 목표

- 플레이어의 음성/행동/월드 상태를 입력으로 받아, NPC가
  - 자연스러운 음성 대사
  - 일관된 감정 표현 및 제스처
  - 게임 월드 내 행동(StateTree/Smart Object 연동)
  을 실시간에 가깝게 수행하도록 함.
- 클라우드 의존 없이 **로컬 머신 단독**으로 실행 가능해야 함.

### 2.2 비기능 요구사항

- 지연(latency)
  - 플레이어 발화 종료 → NPC 음성 재생 시작까지 **1–3초 내**를 목표
  - TTS 자체는 약 **300ms 전후**(VibeVoice-Realtime-0.5B 또는 동급 모델 기준)를 목표
- 안정성/복원력
  - 개별 모듈 장애(LLM/TTS/ASR)가 발생해도 오케스트레이터가 에러를 포착하고 기본 대사/행동으로 graceful fallback
- 모듈성
  - LLM, ASR, TTS는 **서로 독립된 서비스**로 배치하며, 교체·확장 가능해야 함
- VR 특화
  - 모바일 HMD(Quest 3 등)에서는 무거운 연산/통신을 직접 처리하지 않고, **PC 중계 서버(오케스트레이터)**가 대부분의 추론을 담당

---

## 3. 런타임 아키텍처 개요

### 3.1 상위 구조

```

[Quest 3 / UE5 Client]
│  (WebSocket, JSON + Audio)
▼
[Orchestrator (Python)]
│        │        │
▼        ▼        ▼
[LLM]   [ASR/Audio-LLM]   [TTS]

```

- UE5 클라이언트
  - 월드 컨텍스트(StateTree, Smart Object, 플레이어 위치·상태)를 JSON으로 보내고, NPC 행동 명령을 JSON으로 받음
  - 오디오 스트림은 WebSocket URL 또는 오디오 청크로 수신 후 재생
- Orchestrator
  - WebSocket 서버 (UE5와 직접 통신)
  - LLM/ASR/TTS HTTP/WebSocket 클라이언트 역할
  - 프롬프트 구성, 결과 파싱, 오류 처리 및 StateTree 액션 매핑 담당
- LLM 서비스
  - Qwen, Gemma, Llama 등 로컬 LLM
  - OpenAI 호환 /v1/chat/completions API로 orchestration
- ASR/오디오 LLM
  - Whisper 계열 ASR 또는 Gemma 4 E2B/E4B 같은 오디오 네이티브 LLM
  - 목적: 플레이어 음성을 텍스트 또는 구조화된 의미 표현으로 변환
- TTS 서비스
  - VibeVoice-Realtime-0.5B, NeuTTS Air 등
  - 텍스트 + 감정 → 실시간 음성 스트림

### 3.2 Docker Compose 예시

```yaml
version: '3.8'
services:
  llm-service:
    image: vllm/vllm-openai:latest
    command: --model unsloth/Qwen3.5-32B-GGUF --port 8000
    ports: ["8000:8000"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  asr-service:
    image: local/whisper-asr:latest
    ports: ["8002:8002"]

  tts-service:
    image: local/vibevoice-realtime-0.5b:latest
    ports: ["8001:8001"]

  orchestrator:
    build: ./orchestrator
    ports: ["8765:8765"]
    depends_on: [llm-service, asr-service, tts-service]
```


---

## 4. 공통 메시지 스키마 (WebSocket JSON)

### 4.1 UE5 → Orchestrator: 플레이어 입력

```json
{
  "type": "player_input",
  "client_id": "quest3_player_01",
  "timestamp": 1715000000,
  "world_context": {
    "npc_id": "NPC_Guard_01",
    "current_state": "Idle",
    "player_state": {
      "position": [0.0, 0.0, 0.0],
      "rotation_yaw": 180.0,
      "is_in_combat": false
    },
    "nearby_smart_objects": [
      {
        "id": "WeaponRack_02",
        "type": "WeaponRack",
        "distance": 120.0
      },
      {
        "id": "Door_01",
        "type": "Door",
        "distance": 250.0
      }
    ]
  },
  "input_payload": {
    "mode": "audio",
    "audio_base64": "<Base64_PCM>",
    "text_fallback": null
  }
}
```

- `mode`
    - `audio`: 오디오 스트림 기반 입력 (ASR 또는 오디오 LLM 사용)
    - `text`: 텍스트 직접 입력 (디버깅 또는 채팅 UI용)


### 4.2 Orchestrator → UE5: NPC 행동/음성 응답

```json
{
  "type": "npc_action_response",
  "request_id": "req_20240510_001",
  "npc_id": "NPC_Guard_01",
  "dialogue_text": "이 구역은 통행이 금지되어 있다.",
  "audio_stream": {
    "mode": "websocket",
    "url": "ws://pc-host:8001/ws/tts/stream/req_20240510_001"
  },
  "animation_metadata": {
    "emotion": "serious",
    "gesture": "point_right",
    "look_at_player": true
  },
  "statetree_actions": [
    {
      "action": "LookAt",
      "target": "Player",
      "params": {
        "interp_time": 0.4
      }
    },
    {
      "action": "UseSmartObject",
      "target": "WeaponRack_02",
      "params": {
        "interaction_type": "Point"
      }
    }
  ]
}
```


---

## 5. 모듈별 상세 명세

### 5.1 LLM 서비스

#### 5.1.1 API 인터페이스 (OpenAI 호환)

- Endpoint: `POST /v1/chat/completions`

```json
{
  "model": "Qwen3.5-32B-GGUF",
  "messages": [
    {"role": "system", "content": "..."},
    {"role": "user", "content": "..."}
  ],
  "temperature": 0.7,
  "response_format": {"type": "json_object"}
}
```

- 응답은 반드시 JSON 문자열이어야 하며, Orchestrator가 `json.loads()`로 파싱 가능해야 함.


#### 5.1.2 시스템 프롬프트 규약 (예시)

```json
{
  "dialogue_text": "NPC가 말할 대사",
  "emotion": "happy|sad|angry|serious|neutral",
  "actions": [
    {
      "action": "UseSmartObject|MoveTo|LookAt|PlayMontage",
      "target": "타겟 식별자 (예: WeaponRack_02, Player)",
      "params": {}
    }
  ]
}
```

- 시스템 프롬프트 내부에 위 JSON 포맷을 자연어로 설명하고, **어떠한 추가 문장도 없이 해당 JSON만 출력**하도록 지시.


### 5.2 ASR / 오디오-네이티브 LLM

#### 5.2.1 모드 A: ASR + 텍스트 LLM

- ASR 서비스
    - Endpoint: `POST /v1/asr/transcribe`

```json
{
  "audio_base64": "<Base64_PCM>",
  "language_hint": "ko-KR"
}
```

- Response

```json
{
  "text": "저기 있는 문 좀 열어줄래?",
  "confidence": 0.93
}
```

- Orchestrator는 `text`를 LLM의 user 메시지 일부로 사용.


#### 5.2.2 모드 B: 오디오-네이티브 LLM (Gemma 4 E2B/E4B 등)

- Endpoint: 모델별 규약에 따르되, Orchestrator 추상화 레벨에서는
    - 입력: `audio_base64` + `world_context`
    - 출력: `transcript` + `semantic_intent`(옵션)

```json
{
  "transcript": "저기 있는 문 좀 열어줄래?",
  "intent": "REQUEST_OPEN_DOOR",
  "slots": {
    "target_door": "Door_01"
  }
}
```

- 이 경우 LLM 호출을 생략하거나, LLM은 추가적인 대사 스타일링/내적 독백 등에만 사용 가능.


### 5.3 TTS 서비스 (VibeVoice / NeuTTS Air 등)

- REST 단발 엔드포인트
    - `POST /v1/tts/synthesize`
- WebSocket 스트리밍
    - `GET /ws/tts/stream/{request_id}`
- 요청 파라미터
    - `text`: 대사 문자열
    - `voice_id`: 화자/프리셋 ID
    - `emotion`: LLM에서 나온 감정 태그를 음색/프로소디에 매핑
    - `speaking_rate`, `pitch`: 말 속도/피치 조절
- Orchestrator는 TTS 요청 후, UE5에 전달할 `audio_stream.url` 또는 스트리밍 핸들을 생성.


### 5.4 Orchestrator (Python WebSocket 서버)

#### 5.4.1 핵심 책임

1. UE5 WebSocket 수신 및 세션 관리
2. 플레이어 입력에서 음성·텍스트를 추출하고 ASR/오디오 LLM 호출
3. LLM을 이용해 NPC 대사/행동 계획 생성
4. TTS를 호출해 음성 스트림 생성
5. UE5로 `npc_action_response` JSON 전송
6. 오류/타임아웃 처리 및 fallback 응답 제공

#### 5.4.2 의사 코드 (요약)

```python
async def handle_session(websocket):
    async for msg in websocket:
        data = json.loads(msg)
        if data.get("type") != "player_input":
            continue

        # 1. 입력 모드 분기
        if data["input_payload"]["mode"] == "audio":
            user_text = await call_asr_or_audio_llm(data["input_payload"])
        else:
            user_text = data["input_payload"]["text_fallback"]

        # 2. 프롬프트 구성
        world = data["world_context"]
        prompt = build_prompt(world, user_text)

        # 3. LLM 호출
        llm_result = await call_llm(prompt)
        # llm_result: {dialogue_text, emotion, actions}

        # 4. TTS 호출
        audio_stream_url = await call_tts(llm_result["dialogue_text"], llm_result["emotion"])

        # 5. UE5 응답 구성
        response = {
            "type": "npc_action_response",
            "npc_id": world["npc_id"],
            "dialogue_text": llm_result["dialogue_text"],
            "audio_stream": {
                "mode": "websocket",
                "url": audio_stream_url,
            },
            "animation_metadata": {
                "emotion": llm_result["emotion"],
                "gesture": "auto_gesture"
            },
            "statetree_actions": llm_result["actions"]
        }

        await websocket.send(json.dumps(response))
```


---

## 6. UE5 클라이언트 통합 명세

### 6.1 WebSocket 클라이언트 레이어

- 연결 규약
    - URL: `ws://{orchestrator_host}:8765/ws`
    - 재연결 로직: 연결이 끊어질 경우 지수 백오프 기반 재시도
- 이벤트
    - `OnConnected`, `OnMessage`, `OnError`, `OnClosed`
- 전송
    - StateTree/Smart Object 상태를 틱/이벤트 기반으로 수집해 `player_input` 메시지 생성


### 6.2 StateTree \& Smart Object 연동

- `statetree_actions` 배열 파싱 후, 각 액션을 UE5 StateTree 이벤트로 매핑

예시:

```json
{
  "action": "UseSmartObject",
  "target": "WeaponRack_02",
  "params": {"interaction_type": "Inspect"}
}
```

- C++/Blueprint 구현
    - JSON → UStruct 변환 (Action, Target, Params)
    - Target ID를 월드 내 Actor 레퍼런스로 resolve (Smart Object 시스템)
    - StateTree Blackboard Key에 Target 설정 후, 해당 액션 전용 StateTree 태스크/전환 트리거


### 6.3 오디오 및 애니메이션 파이프라인

- 오디오 재생
    - `audio_stream.mode == "websocket"`인 경우, 지정된 URL로 추가 WebSocket 연결을 열어 오디오 청크 수신
    - Base64 디코딩 후 `USoundWaveProcedural` 또는 Runtime Audio Importer 플러그인으로 재생
- Lip Sync / Facial Animation
    - MetaHuman Lip Sync, Audio2Face 등으로 오디오 스트림을 직접 공급
    - `animation_metadata.emotion`을 AnimBlueprint의 파라미터로 주입해, 12가지 Mood 또는 BlendSpace와 연결
- 제스처/바디 모션
    - `animation_metadata.gesture`에 따라 지정된 Montage 또는 애님 시퀀스를 재생

---

## 7. 단계별 개발 로드맵 (Phase 0–4)

### Phase 0 – 텍스트 기반 WebSocket 프로토타입

- 목표
    - UE5 ↔ Orchestrator 간 WebSocket JSON 왕복 검증
    - LLM/TTS 없이, 하드코딩된 `npc_action_response`를 반환
- 산출물
    - 최소 JSON 스키마 정의
    - UE WebSocket 클라이언트 래퍼
    - Python WebSocket 에코 서버


### Phase 1 – 월드 컨텍스트 + StateTree 연동

- 목표
    - `world_context`와 `statetree_actions` 설계를 완료하고, LLM을 액션 플래너로 활용
- 작업
    - LLM 시스템 프롬프트에 JSON 스키마와 허용 행동 목록 정의
    - UE에서 `statetree_actions`를 파싱해 StateTree/Smart Object를 실제로 제어


### Phase 2 – 로컬 LLM/TTS 도커 스택 통합

- 목표
    - docker-compose 기반 LLM + ASR + TTS + Orchestrator 스택 구축
- 작업
    - vLLM(or equivalent)로 LLM 서비스화
    - VibeVoice/NeuTTS Air로 TTS 서비스화
    - Orchestrator가 HTTP/WebSocket 클라이언트로 이를 호출하도록 구현


### Phase 3 – VR/Quest 3 최적화 \& 멀티 NPC

- 목표
    - Quest 3 + PC 조합에서 실 플레이에 견딜 수 있는 지연/성능 확보
- 작업
    - 네트워크 RTT 측정 및 지연 예산 분배 (ASR, LLM, TTS, UE)
    - client_id, npc_id 기반 멀티 세션 관리
    - 근접/시야 기반 대화 시작·종료 로직 추가


### Phase 4 – 툴링 및 운영

- 목표
    - 개발자/디자이너가 함께 사용할 수 있는 AI NPC 저작/디버그 툴 제공
- 작업
    - Web UI 또는 UE Editor Utility로 메시지/프롬프트/응답 로그 뷰어 구현
    - 캐릭터별 프롬프트/행동 템플릿 편집기
    - 재현 가능한 리플레이 모드 (과거 player_input 로그 재생)

---

## 8. 품질/테스트 체크리스트

1. 지연 측정
    - ASR, LLM, TTS, WebSocket, UE 렌더링 각각의 지연 및 전체 E2E 지연 측정
2. 안정성
    - 모듈 장애/타임아웃 시 fallback 대사/행동 정상 동작 여부
3. 행동 일관성
    - 동일한 world_context + 플레이어 발화에서 NPC 행동이 과도하게 랜덤하지 않은지 검증
4. VR UX
    - 플레이어 시점에서 대기 시간, 오디오 품질, NPC 반응 타이밍이 자연스러운지 플레이 테스트

---

## 9. 확장 포인트

- 메모리/지식 베이스
    - NPC별 장기 기억(플레이어와의 과거 상호작용, 스토리 진행 상황)을 LLM 컨텍스트 외부에 저장하고, 필요 시 프롬프트에 주입
- 시각/제스처 인식
    - 카메라/컨트롤러 데이터를 활용한 행동 인식 모델을 Orchestrator에 추가해, 플레이어의 손짓/시선에 따른 동적 반응
- 멀티플레이 동기화
    - 여러 플레이어가 동일 NPC와 상호작용할 때, 서버 권위(authoritative) 구조로 NPC 상태를 동기화

---

이 명세서는 **엔진 팀과 AI 서버 팀 간의 공용 계약서(Contract)**로 사용하는 것을 전제로 작성되었습니다.
각 팀은 이 문서를 기준으로, 구현 언어나 내부 구조를 자유롭게 선택하되, 여기 정의된 메시지 스키마와 API 규약을 최대한 유지하는 것을 권장합니다.

```

이걸 `local-vr-npc-architecture.md`로 저장해서 쓰면 되고, TTS 레이어만 분리한 `vibevoice-dev-spec.md`랑 같이 두면 **전체 아키텍처 + 모듈별 상세 명세**가 깔끔하게 나눠질 거예요.```

