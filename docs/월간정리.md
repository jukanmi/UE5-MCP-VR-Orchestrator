# 월간 정리 — 2026-01 ~ 2026-09

출처: `docs/주간기록/` 28개 파일(W03~W37) + git log. 갱신 2026-09-17.
주간기록이 1차 소스, 이 문서는 월 단위 롤업. 상세(커밋 해시·함정·설계 근거)는 각 주차 파일 참조.

---

## 2026-01 — 골격 세우기 (W03~W04)

- UE5 VR 프로젝트 + FastAPI **Cognitive Engine** 첫 가동, WebSocket UE5↔Python 연결
- **LLM Factory** 다중 백엔드(Gemma/OpenAI), Action Schema 확립
- 이론 골격: **4-Agent 정의**(Narrative / Character / Rules / Interface) — 이후 코드 청사진
- 설계 전환: *Rules 에 LLM 불필요* → Pydantic 코드 검증으로 강등
- 정책 기반 **하이브리드 아키텍처**(빠른 분류 + 느린 추론) 착수

## 2026-02 — NPC 코어 (W05~W09)

- VR Player 입력, **SmartNPC + BT/Blackboard** 1차 구현
- **3계층 두뇌 모델 확립**: 대뇌(LLM 전략) / 소뇌(SLM 100ms) / 척수(C++ 0ms) — 이후 모든 작업의 판단 기준
- **AIPerception**(시야·주변시·진영), Linear Pipeline(Interface→Supervisor→Dialogue→Rules→UE5)
- KLUE-RoBERTa 의도 분류 + RAG 도입 → 같은 달 제거(LLM 일원화로 선회)
- **BehaviorMode + Action Queue** 순차 실행 패턴 확립, BTTask 카테고리 헤더 재편
- **SmartNPC Facade 분리** 1060줄 → 332줄 (State/Action 컴포넌트), Durability·ItemManager
- **OmniAgent 통신 프로토콜** 정립(메시지 타입 분리·보안 미들웨어), NPC 소리 발생·인지
- **EmergencyReport 배치 발송**(0.2~0.5s 창, NPC별 1회) — 트래픽 폭주 해소
- DiceSystem 설계(`Success = Stat / Difficulty`), EQS 진영 기반 위치 쿼리

## 2026-03 — 소강 (W10; W11~W13 기록 없음)

- 미지 대상 추론 스킬, NPCAttributes→NPCStatement 분리, WebSocketClient 정리
- **시각·청각은 변화 시에만 송신** 정책 확정
- 중순~말 활동 미미(`temporary`·README 정도)

## 2026-04 — 인간화 + 대청소 (W14, W17~W18; W15~W16 공백)

- **SLM 액션 파이프라인** + 전술 위치 결정(UE5 가 SAFE/OPTIMAL/AGGRESSIVE 3후보 → LLM 선택)
- **NPC 인간화**: 주사위 판정 패닉·전술 가중치 (2월 DiceSystem 설계 실현)
- **대규모 코드리뷰 픽스**: Critical 4 · High 6 · Medium 6 · Low 2 (UB `check()`·WS 누수·stale 패킷·Ollama 타임아웃 등)
- NPCAnimInstance·Death 시스템·Attack 몽타주, BT 완료 경로 동기 단일화
- **EQS 에셋 7→2 통합** → 이후 신규 에셋 금지, Named Parameter 정책(CLAUDE.md §4)
- LLM **importance별 다층 라우팅**(normal/high/core), 로컬 디버그 대시보드 `/debug`

## 2026-05 — StateTree · VR 포팅 · TTS (W19~W22)

- **BT → StateTree 마이그레이션**(MCPStateTreeAIComponent + STTask), EQS 파이프라인 안정화(request_gen stale 차단·이동 중 쿼리 금지·Follow 무한루프 fix)
- **Meta Quest 3S 포팅 Phase 1~2**: OpenXR·VRPawn·Android 패키지(arm64/Vulkan/ASTC)
- **TTS M1 사인파 스텁 → M2 모델 삽질**: VibeVoice(API 비호환) → Piper(한국어 미지원) → XTTS → **OpenVoice v2 + MeloTTS 확정**
- **VR 자세 시스템**(Standing/Crouching/Prone): HMD 캘리브레이션·슈미트 트리거 히스테리시스·동적 캡슐 + **FBIK 풀바디 아바타 완성**(손추적·팔꿈치·시야정렬·부드러운 회전)
- **액션 파이프라인 비동기 완료 모델 전환** — 큐가 1프레임에 드레인되던 핵심 결함 수정(MaxActionDuration 워치독 포함)
- 중복 제거 리팩토링(`ComputePerceptionDanger`·`ClearActiveActionState`·`GameplayTagUtils`), **BehaviorMode orphan 실버그** 수정
- OmniAgentConfig(서버 주소 `DefaultGame.ini` 이전), 플레이어→NPC 단순 대화 경로 연결
- gemma SLM thinking 우회(2200ms/289토큰 → 400ms/4토큰)

## 2026-06 — 멀티 NPC · 지연 · VR 전투 (W24, W26; W23·W25 기록 없음)

- **멀티 NPC 병렬 대화**: NPC별 e4b `asyncio.gather` — 지식 누출·감정 수렴 구조적 차단(실측 3/3 격리, `gather wall ≈ max개별`)
- **계획 캐싱**: "비싸게 계획 1회, 싸게 실행 N회" — cold=풀 파이프라인, warm=e4b 단독, C++ `ShouldReplan` 판정
- **지연 최적화**: SLM Reflex raw 전환 warm 330ms · prompt prefix 재배치 183→37ms · core 26b→gemma4-12b · 메모리 기록 fire-and-forget
- **TTS 문장단위 스트리밍**: producer↔consumer 파이프라인 → TTFA 길이 무관 200~330ms(목표 <600ms 달성), 동시 발화 `_synth_lock` 직렬화
- NPC WorldSpace **말풍선 자막**, Stage1 **구조화 출력**, Rules Self/Enemy 센티넬
- **RAG 지식 4 NPC**(Skadi/Moca/Elara/James) persona + lore 작성·FAISS 인덱싱·격리 실측
- 플레이어 HUD·음성입력·NPC 조준, ruff line-length 120 고정
- **TTS CosyVoice2 마이그레이션 → 롤백**(16GB VRAM 과다, MeloTTS+OpenVoice 복귀), ASR CPU 전환·HF 캐시 4.3GB 회수, KV 양자화 불가 확정(FA 커널 크래시)
- **VR 실감형 전투 §3/§4/§5**: 손 스윙 운동에너지 ½mv²·투사체·부위 배율(머리 2.0/사지 0.75)·햅틱·패시브 래그돌·**액티브 래그돌**(Flinch / 넉다운→기상)

## 2026-07 — 진단 · DX · 가구 · 파인튜닝 M2 · "누워"→Sleep · MCP 연결 (W27~W31)

- **Gemma 역할 진단 4종**(라이브 Ollama repro): Stage2 12B 정제 불능 확정 → **plan 전용 축소**, e4b facial 편향 교정, 메모리 요약 한국어화, action target enum grammar 강제
- **P1~P5 리팩토링**(behavior-preserving) + **테스트 회귀 기준선** 확보(27 → 37 → 40 passed)
- **DX 인프라 주간**: 계약 동기화 pytest 9종(C++ 헤더 텍스트 파싱, 빌드 불필요), Live WS 왕복 스모크, GitHub Actions CI, PR 게이트 훅 — 첫날 cp949 print 크래시 잠복 버그 실증 적발
- `valid_targets` 런타임 검증 전환(등록 NPC 액션이 조용히 삭제되던 버그), `style` 키 C++ 소비 구현, 레거시 일괄 제거(`raw_response`·`intent.py` 등)
- **전투 셀렉터**: 무한공격 원인(Attack 100% 자동주입 + 종료 경로 부재) 진단 → SPEC → C++ 척수 셀렉터·타겟 사망 게이트·시야 소실 타임아웃 구현·PIE 검증·PR #19 머지
- **가구 상호작용 시스템 전체 신설**: FurnitureActor/Manager(등록·점유·SeatPoint), 가구 ID 타겟 해석, Sit/Sleep/Read/Pray, AnimBP 자세 유지 상태머신, `nearby_furniture` 인지 컨텍스트, 플레이어 착석 토글 — 원 버그("NPC 앉은 의자에 플레이어 착석") 근본 수정(`bOccupied` 게이트). PIE 전항 통과
- **파인튜닝 데이터 파이프라인 착수**: 학습 로그 jsonl 훅(msg_id 상관), LIGHT 어댑터·한국어화·KREW/multichar 추출, 서빙 정합 JSONL 재작성, 페르소나 풀 300·30:70 혼합
- **"누워"→Sleep 미발화 3-track 수정(W30)**: few-shot 정식 예시 승격, C++ 가구 타입 기반 자세 결정(Bed→LieDown, Seat→SitDown), 학습데이터 편향 해소(Sit 233:Sleep 0 → Sit 193:Sleep 252)
- **파인튜닝 M2(Stage1 e4b) 완주(W30)**: LoRA r32 학습 → GGUF Q4_K_M → Ollama 등록 및 `MODELS` 스위치, gold 액션 재현율 3/8 → 8/8 도약
- **Stage1 v2 재학습 완주(W31)**: 데이터 결함 청산(`독수리`→`독복원` 전역치환 오기 등 수정), v1 오버핏 우려 해소
- **UE5 에디터 MCP 연결(W31)**: `RemoteControl` 프록시 기반 프로퍼티 읽기/쓰기. 에디터 배치 가구(문서 3개 vs 실물 침대 1개) 오기 즉시 적발
- **Stage2 12B plan 보류 확정(W31)**: 상류 지원(Unsloth 12B 미지원) 공백 판단에 따른 보류 결정

## 2026-08 — 기상 보강 · 데이터 파이프라인(3806행) · 에디터 브리지 · 척수반사 · 아이템 레지스트리 (W32~W35)

- **기상(StandUp) 액션 보강(W32)**: "누워"(Sleep)에 대응하는 기상 경로 부재 발견. 재학습 대신 few-shot 프롬프팅으로 우회(비용 대비 효과 판단)
- **학습데이터 3806행 대규모 확장(W33)**: 데이터 생성기 구조 전면 개편. 8대 상황 묘사, negative 패턴(4→7종), 복합 액션(`multi.yaml`) 신설, 701행(v2) → 3806행(5.4배) 완성
- **에디터 파이썬 브리지(W34)**: `ue_run_python` 구현으로 에디터 내 `unreal` 모듈 직접 제어. 에셋 생성·컴포넌트 추가·컴파일·저장 자동화 검증
- **llm-perf 폐기 및 1파일 간이 벤치마크 재작성(W34)**: 무거운 외부 프레임워크 걷어내고 경량화
- **Stage1 v3 배포(W34)**: 3806행 학습 모델 정합 배포
- **C++ 척수반사 테이블 신설(W34)**: 피격·위협 시 330ms의 SLM Reflex(소뇌)조차 느리다는 판단으로 C++ `FReflexRule` 테이블 0ms 결정론적 처리로 전환, VRAM 1.8GB 회수
- **플레이어 시스템 관통(W35)**: Sprint, 인벤토리 HUD 토글, 월드 픽업, `ExecuteGiveItem` 아이템 증발 결함 수정
- **주석 자립화(W35)**: 외부 문서 열람 없이 C++ 헤더/구현체 자체로 설계 의도 파악 가능하도록 주석 개편
- **아이템 레지스트리 72종 구축(W35)**: 7개 카테고리 72종 데이터 테이블 구축 및 NPC 인벤토리 연결

## 2026-09 — 아이템 파이프라인 · VR 손 쥐기 일원화 · 음성 폐기 · 대규모 리팩토링 · SoL-Pi (W36~W37)

- **아이템 사용·드랍·장착 파이프라인(W36)**: `SPEC_item_pipeline` 관통(아이콘 72종, ID 드롭다운, 수량 API, 스택 상한 10), 아이템 복제·증발 결함 3건 수정
- **Hunyuan3D-2 기반 3D 에셋 72종(W36)**: 메시·단순 콜리전·PBR 텍스처 임포트 완료, NPC 아이템 인지 연동(GiveItem/HandObject)
- **VR 손 쥐기 일원화(W37)**: 무기(`EquipItem`)/일반템(`TakeItemToHand`) 이원화 폐기 → 모든 쥔 물건을 물리 액터(`ADroppedItemBase`)로 단일화, 왼손 그립·양손 독립 쥐기, 인벤토리 개폐 기반 던지기/회수 규칙 확립
- **음성 파이프라인(ASR/TTS) 전격 폐기(W37)**: 개발 불편 유발 및 GPU VRAM 점유(~4GB) 해소를 위해 ASR/TTS 제거, HUD 텍스트 채팅(`ChatInput`)으로 대체
- **리팩토링 1~6단계 완주(W37)**: `SPEC_refactor_encapsulation` 기반 죽은 코드 12건 삭제, 세 벌→한 벌 단일화, 캡슐화, Python 구조화(main.py 931→658줄), Envelope 직결, 래그돌 컴포넌트 분리(SmartNPC 795→339줄), pytest 51개 전체 통과
- **SoL-Pi & Mesh Doctor 하네스(W37)**: 에러 슬라이싱 및 UAT 자동화 `tools/sol_pi.py`, 3D 에셋 진단 `tools/mesh_doctor.py`, 3-Tier 에이전트 가이드(`AGENTS.md`) 확립
- **Stage2 플래너 12B → 8B(qwen3:8b) 교체(W37)**: 12B의 `Moca`→`Maca` 환각 버그 차단 및 0.8s 초고속 안정화

---

## 기록 공백

- **W11~W13**(3월 중순~말) · **W15~W16**(4월 초중순) · **W23**(6/2~6/8) · **W25**(6/16~6/22): 주간기록 파일 없음 (소강 및 병행 작업)
