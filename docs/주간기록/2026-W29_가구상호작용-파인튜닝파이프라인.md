# W29 (2026-07-12 ~ 07-18) — 가구 상호작용 시스템·파인튜닝 데이터 파이프라인

<!-- 07-18 기준 작성. 전반: combat_selector 후속 마감 + 가구 상호작용(Sit/Sleep/Read/Pray) 전체 신설·PIE 검증. 후반: LLM 파인튜닝 데이터 파이프라인(학습 로그 훅·한국어화·골든시드) 착수. -->

## 핵심
이번 주는 두 개의 큰 축. ① **가구 상호작용 시스템 전체 신설**(07-17) — `FurnitureActor`·`FurnitureManager`(등록·점유·SeatPoint) 부터 가구 ID 타겟 해석·Sit/Sleep/Read/Pray 연동(도착 스냅·점유 반납·무타겟 무동작 방어)·AnimBP 자세 유지 상태머신·반경 내 `nearby_furniture` 인지 컨텍스트·플레이어 착석 토글까지 UE5↔Python 양단 관통. **원 버그("NPC 앉은 의자에 플레이어 착석")의 근본 수정** = 점유(`bOccupied`) 게이트. 다음날 제단·책상 가구는 제거하고 Read/Pray 를 제자리 액션으로 단순화(07-18). 가구 PIE 검증 전항 통과(2-1·2-2·2-3·2-6, DoList Done). ② **파인튜닝 데이터 파이프라인 착수**(07-18) — Stage1/2·Rules 학습 로그 jsonl 훅(msg_id 상관), LIGHT 어댑터·한국어화·KREW/multichar 추출 파이프라인, 학습 JSONL 서빙 정합 재작성(DialogueResponse 전체 타겟·Rules 게이트·실페르소나 리맵).

주 앞머리는 W28 잔여 마감: R4·R3 Python 리팩토링 PR #20 머지 + combat_selector 후속 fix 2건(MoveTo 즉시결과 동기 처리·자세 플래그 재생 성공 시에만·WS 종료 시 태스크 취소·호감도 쓰기 shield).

## 주요 작업

### combat_selector·R4/R3 마감 (07-12, `1a1599c`·`b368dc6`·`db42c6d`)
- **MoveTo 즉시결과 동기 처리** — 이미 사거리 내일 때 동기 완료 누락 수정. 자세 플래그는 **몽타주 재생 성공 시에만** 세팅(실패 시 유령 자세 방지).
- **WS 종료 shield** — 소켓 종료 시 진행 중 백그라운드 태스크 취소 + 호감도 쓰기 shield(종료 레이스 방지).
- **R4·R3 리팩토링 PR #20 머지** — 브랜치 `refactor/python-r4-r3`(async_tasks 단일화·프롬프트 모듈 분리·natural_context 헬퍼 추출). behavior-preserving, pytest 40 passed.

### 가구 상호작용 시스템 (07-17, `1e72f21`~`299f1bc`)
- **가구 액터·매니저**(`1e72f21`): `FurnitureActor`(부모, 이벤트 그래프 0노드) + `FurnitureManager` — 등록·점유(`bOccupied`)·SeatPoint. BP_Chair·BP_Bed 생성·레벨 배치(실 ID: 의자 1개 + 침대 `1002` — 2026-07-31 MCP 에디터 실측 정정, 기재했던 `1001`·`1002`·`Bed_01` 3개는 오기).
- **가구 ID 타겟 해석**(`6aef117`): `STTask_ExecuteSmartAction::ResolveActionTarget` 에 가구 조회 분기.
- **Sit/Sleep/Read/Pray 연동**(`e8cdb87`): 도착 스냅·점유 획득·완료 시 반납·**무타겟이면 무동작 방어**(가구 없는 곳 "앉아" → 대사만). BaseMove AlreadyAtGoal 수정으로 가구 옆 즉시 착석(15초 지연 제거).
- **자세 유지 상태머신**(`4b26ed9`): `NPCAnimInstance` 가 `bIsSit`/`bIsLie` 를 NPCStateComponent 에서 미러 → `ABP_SmartNPC` Sit/Lie 상태머신(몽타주=진입 모션, 상태머신=유지). SitDown 종료 후 앉은 자세 유지.
- **가구 인지 컨텍스트**(`0773b04`·`299f1bc`): `NPCManager` 가 반경 내 `nearby_furniture` 동봉(Seat/vacant/거리) + `valid_targets` 는 **빈 가구 한정**(전 점유 시 target 지정 불가 → "자리 없다" 대사). Python 측 `nearby_furniture` 스키마·컨텍스트 주입·few-shot(envelope·vr_context·main·interface_input·prompts).
- **플레이어 착석 토글**(`8dba880`): VR A버튼·PC E키 착석/기상, SeatPoint 스냅 + 이동·점프 잠금. **NPC 점유 의자엔 착석 불가**(원 버그 근본 수정).

### 제단·책상 제거 + Read/Pray 제자리화 (07-18, `b298f66`)
- 제단·책상 가구 에셋 제거 → Read/Pray 를 가구 이동 없는 **제자리 액션**으로 단순화(대상 바라보기 + 몽타주).

### 파인튜닝 데이터 파이프라인 (07-18, `ee71043`·`fcb076f`·`f1acd8b`)
- **LLM 학습 로그 훅**(`ee71043`): Stage1/2·Rules 를 `msg_id` 상관으로 jsonl 기록(`train_logger`·llm_factory·dialogue·rules). 실 대화 트래픽을 학습 코퍼스로 수집.
- **파인튜닝 데이터 파이프라인**(`fcb076f`): LIGHT 어댑터·한국어화·KREW/multichar 추출(`finetune/`).
- **학습 JSONL 서빙 정합 재작성**(`f1acd8b`): DialogueResponse **전체 타겟**·Rules 게이트·실페르소나 리맵 — 학습 타겟이 서빙 시 실제 출력과 정합하도록 generate_stage1/2·light_quests·골든시드 재작성.

## 메모 (Memo.md Handoff / DoList, 07-17~18)
- **가구 BP = 이벤트 그래프 0노드**: 부모 `FurnitureActor`, 컴포넌트·프로퍼티만. SeatPoint Z ≈ 좌판 + 캡슐 HalfHeight(88).
- **점유 게이트가 원 버그 근본 수정**: "NPC 앉은 의자에 플레이어 착석" = `bOccupied` 미존재였음. NPC 사망·넉다운 시 `bOccupied=false` + `StopAllActions` 자세 해제.
- **PIE 선행**: `nearby_furniture` 스키마 추가로 Python 서버 재시작 필수(스키마 반영).

## 검증 완료 (07-18, DoList Done)
- **가구 인지·무타겟 방어**(2-1): natural_context `Nearby furniture 1001 (Seat, vacant)` + Stage1 `{"type":"Sit","target":"1001"}` → 이동·스냅·착석. 가구 없는 곳 무동작+대사만. 전 점유 시 target 지정 불가.
- **가구 NPC 착석 기본**(2-2): `[FurnitureManager] Registered`·즉시 착석·둘째 NPC 점유 불가·사망 시 반납·삭제 후 재지시 크래시 없음.
- **가구 플레이어 착석**(2-3): SeatPoint 스냅+입력 잠금·재입력 기상·회귀 없음.
- **이전 커밋**(2-6): PlaySound(`b4b0939`) 소리·Lifestyle 글리치(`f0b2788`) 제자리 재생.

## 검증 대기 (다음 주)
- Read/Pray 제자리 몽타주(2-4)·"누워" LieDown 유지(2-5).
- **Stage2 골든 시드 92개 검수**(DoList 2.5) — 알려진 누출(~25%: "~시키고"·"한다."·"시스템" 현대어).

## 커밋
| 날짜 | 해시 | 메시지 |
|------|------|--------|
| 07-11 | `607cb99` | refactor: R4 잔짐 — fire-and-forget 공용 헬퍼 단일화·프롬프트 분리 |
| 07-11 | `3be0d48` | refactor: R3 interface_input 분해 — _format_* 조각·노드 가드 헬퍼 추출 |
| 07-11 | `ff4774d` | refactor: 예약 파라미터 키 snake_case 통일 — §1 정합 |
| 07-11 | `b4b0939` | feat: 액션 미디어 사운드 재생 — PlaySoundAtLocation, TODO 스텁 해소 |
| 07-11 | `f0b2788` | fix: Lifestyle 이동+몽타주 동시실행 글리치 — 이동 Sit/Sleep 한정·도착 후 재생 |
| 07-12 | `1a1599c` | fix: MoveTo 즉시결과 동기 처리·자세 플래그 재생 성공 시에만 |
| 07-12 | `b368dc6` | fix: WS 종료 시 진행 태스크 취소·호감도 쓰기 shield |
| 07-12 | `db42c6d` | Merge pull request #20 (refactor/python-r4-r3) |
| 07-17 | `1e72f21` | feat: 가구 액터·매니저 신설 — 등록·점유·SeatPoint + BP·레벨 배치 |
| 07-17 | `6aef117` | feat: 가구 ID 타겟 해석 — ResolveActionTarget 가구 조회 분기 |
| 07-17 | `e8cdb87` | feat: Sit/Sleep/Read/Pray 가구 연동 — 스냅·점유·반납·무타겟 방어 |
| 07-17 | `4b26ed9` | feat: 자세 유지 — bIsSit/bIsLie AnimBP 미러·Sit/Lie 상태머신 |
| 07-17 | `0773b04` | feat: 가구 인지 컨텍스트 — nearby_furniture 동봉·valid_targets 빈 가구 한정 |
| 07-17 | `8dba880` | feat: 플레이어 착석 토글 — VR A·PC E키·이동·점프 잠금 |
| 07-17 | `299f1bc` | feat: nearby_furniture 스키마·컨텍스트 주입·가구 few-shot |
| 07-18 | `b298f66` | refactor: 제단·책상 가구 제거 — Read/Pray 제자리 액션화 |
| 07-18 | `ee71043` | feat: LLM 학습 로그 훅 — Stage1/2·Rules msg_id 상관 jsonl 기록 |
| 07-18 | `fcb076f` | feat: 파인튜닝 데이터 파이프라인 — LIGHT 어댑터·한국어화·KREW/multichar 추출 |
| 07-18 | `f1acd8b` | fix: 학습 JSONL 서빙 정합 재작성 — DialogueResponse 전체 타겟·Rules 게이트·실페르소나 리맵 |
