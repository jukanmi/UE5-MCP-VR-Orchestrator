# W32 (2026-08-03 ~ 08-09) — 기상(StandUp) 액션 누락 보강 · 재학습 대신 few-shot 대체

<!-- 커밋 1건(b5675ef)만 있는 조용한 주. Memo.md Done 에는 항목이 없어 커밋 실물 기준으로 소급 작성(2026-08-29). -->

## 핵심

W30 에서 "누워"(Sleep)를 고쳤더니 그 반대인 **"일어나"(기상)에 대응하는 함수가 아예 없다는 것**이 테스트 중 드러났다. 앉기/눕기는 있는데 그 상태에서 빠져나오는 경로가 비어 있던 것.

주목할 판단은 **재학습을 하지 않기로 한 것**이다. Stage1 v2 학습이 끝난 지 며칠밖에 안 된 시점이라, 어휘 하나 때문에 학습 사이클을 다시 돌리는 대신 **few-shot 프롬프팅으로 우회**하고 학습용 시드 데이터만 미리 심어뒀다(다음 사이클에서 자연히 흡수되도록). 비용 대비 효과 판단이 명시적으로 이뤄진 사례.

이 커밋에는 **의자 액터 삭제**도 함께 들어갔는데, 이 사실이 이후 오래 오기로 남아 혼선을 만들었다(아래 메모).

## 주요 작업

### 기상(StandUp) 액션 추가 (`b5675ef`)

- **C++** (`NPCActionComponent.cpp`, +54줄) — 기존 앉기/일어서기 경로와 연동해 기상 처리 추가. `Config/DefaultGameplayTags.ini` 에 태그 1건 등록.
- **AnimBP** (`ABP_SmartNPC.uasset`) — 대응 상태 반영.
- **프롬프트 우회** (`prompts.py`) — 학습 직후라 재학습 대신 few-shot 예시로 "일어나" 명령 대응. 스키마(`actions.py`)·`llm_factory.py` 소폭 정합.
- **차기 학습 대비** (`scenarios_seed_draft.yaml`, +309줄) — "일어나" 계열 시드 데이터를 미리 추가. 다음 학습 사이클에서 프롬프트 우회를 걷어낼 수 있게 준비.

## 메모

- **재학습 대신 few-shot 은 임시 조치다** — 시드 데이터를 이미 넣어뒀으므로, 다음 Stage1 학습 후에는 `prompts.py` 의 기상 few-shot 을 걷어낼 수 있는지 확인할 것. 방치하면 프롬프트가 계속 길어진다.
- **의자 액터 삭제가 이 커밋에 포함됐다** (`__ExternalActors__/.../9W41SJ8WBRACSF4OAQ81DY.uasset` → 0 bytes). 이후 `Memo.md`·`DoList.md` 에 "워킹트리에서 삭제된 상태(커밋 안 됨)" 로 잘못 기록돼 있다가 2026-08-18 에야 정정됐다 — 추가는 `1e72f21`, 삭제는 이 `b5675ef`. 삭제 자체도 사고가 아니라 **의도된 것**으로, "누워" 지시가 침대를 고르는지 보려면 의자가 후보에서 빠져야 했다. Sit 계열을 다시 검증하려면 의자를 재배치해야 한다.

## 커밋

| 날짜 | 해시 | 메시지 |
|------|------|--------|
| 08-03 | `b5675ef` | fix: 기상 함수 누락 추가 |
